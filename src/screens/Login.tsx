
import React from 'react';
import { Box } from '../utils';

interface LoginProps {

}

export const Login = ({}) => {
   
        return (
            <Box backgroundColor="purple" flex={1}/>
        );
}